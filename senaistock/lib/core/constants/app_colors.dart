import 'package:flutter/material.dart';

class AppColors {
  // Cores Principais
  static const Color primary = Color(0xFFD81B60); // Rosa Principal (Ajuste se necessário)
  static const Color background = Color(0xFFFCE4EC); // Rosa Clarinho de Fundo
  
  // Cores de Texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF666666);
  
  // Cores de Status
  static const Color statusTranquilo = Color(0xFF4CAF50); // Verde
  static const Color statusAlerta = Color(0xFFFF9800);    // Laranja
  static const Color statusCritico = Color(0xFFF44336);   // Vermelho
}